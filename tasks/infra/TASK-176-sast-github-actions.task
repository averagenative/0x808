status: deleted
priority: medium
depends: none
agent: infra
ref: TASK-176
---
SAST via GitHub Actions — REJECTED.

User has a hard preference: zero GitHub Actions on this repo. The
local cppcheck pass shipped via TASK-171 (./scripts/lint.sh) covers
the main SAST need.

If deeper SAST is wanted, the following all run as local CLI tools:
clang-tidy, clang-analyzer (scan-build), infer, semgrep. None
require a hosted workflow.
