status: deleted
priority: medium
depends: none
agent: infra
ref: TASK-150
---
CI matrix builds — REJECTED.

User has a hard preference: zero GitHub Actions on this repo.
Cross-platform build verification stays manual / local. If desired
later, a local `scripts/build_all.sh` could exercise every cmake
target on the dev box, including the MinGW cross-compile path
already in cmake/mingw-w64.cmake.

Do not reintroduce as a workflow.
